# ==============================================
# 数据库配置 (必须)
# ==============================================
# PostgreSQL 数据库连接字符串
# 格式: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# 生产环境示例:
DATABASE_URL="postgresql://user:password@localhost:5432/fileonline?schema=public"

# ==============================================
# 网站基础配置 (必须)
# ==============================================
# 网站的完整访问 URL (用于回调和重定向)
# 本地开发: http://localhost:3000
# 生产环境: https://your-domain.com
NEXTAUTH_URL="http://localhost:3000"

# NextAuth 加密密钥
# 生成命令: openssl rand -base64 32
NEXTAUTH_SECRET="changeme_in_production_secret_key_123456"

# ==============================================
# 文件存储 (Backblaze B2 / MinIO / S3) (必须)
# ==============================================
# 存储类型: minio (兼容 S3 协议)
STORAGE_TYPE="minio"

# B2 / S3 端点地址 (不带 https://)
MINIO_ENDPOINT="s3.us-west-004.backblazeb2.com"

# 端口 (HTTPS通常为443)
MINIO_PORT=443

# 是否使用 SSL (true/false)
MINIO_USE_SSL=true

# 访问密钥 (Access Key ID)
MINIO_ACCESS_KEY="your_access_key"

# 秘密密钥 (Secret Access Key)
MINIO_SECRET_KEY="your_secret_key"

# 存储桶名称
MINIO_BUCKET_NAME="your-bucket-name"

# ==============================================
# 邮件通知 (可选 - 如果不配置则邮件功能失效)
# ==============================================
# SMTP 服务器地址
SMTP_HOST="smtp.example.com"

# SMTP 端口 (通常为 465 或 587)
SMTP_PORT=465

# 是否使用 SSL/TLS (true/false)
SMTP_SECURE=true

# 发件人邮箱账号
SMTP_USER="noreply@example.com"

# 发件人邮箱密码 (或应用专用密码)
SMTP_PASS="your_email_password"

# 发件人显示名称和地址
SMTP_FROM="InsightLink <noreply@example.com>"

#自动创建/提权：
#如果账号不存在 -> 自动用此密码创建一个管理员账号。
#如果账号已存在 -> 自动将该账号的角色提升为管理员 (不会修改现有密码)。
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your_password
ADMIN_NAME=Admin
# ==============================================
# 支付配置 (可选 - 如需 Stripe 支付)
# ==============================================
# STRIPE_SECRET_KEY="sk_test_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."
